"""Python entrypoints and GP-DVRPTW Python port.

This file makes `python_src` an importable Python package so code can be run as:

    python -m python_src.main <dataset.csv>

and tests can import `python_src.*` modules.
"""
